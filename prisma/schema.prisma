generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model FileShare {
  id           String   @id @default(nanoid())
  title     String
  metadata  String
  price     String

  owner     String
  cid       String
} 